collections:
  authors:
    output: true

defaults:
  - scope:
      path: "p8-t3-jekyll-netlify-alu0100836950.github.io"
      type: "authors"
    values:
      layout: "author"
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
